#include "main.h"

/**
 * main - prints its name, followed by a new pine
 * @argc: number of arguments counting
 * @argv: arguments passed to a programm
 * Return: Eachtime 0 success
 */
int main(int argc _atribute_((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
